{
  "name": "anthros-ai",
  "version": "9.0.0",
  "description": "AnthrosAI — AI-powered fitness and nutrition PWA",
  "private": true,
  "engines": { "node": ">=18" },
  "scripts": {
    "dev":   "vercel dev",
    "build": "echo 'Static — no build step'",
    "deploy":"vercel --prod"
  },
  "dependencies": {
    "stripe": "^14.21.0",
    "groq-sdk": "^0.3.3"
  },
  "devDependencies": {
    "vercel": "^37.0.0"
  }
}
